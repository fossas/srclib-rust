// This file is used to allow docker builds to pre-build the dependency cache
// before adding real source code to the image and running the real build.
// It is ignored for normal builds.
fn main() {}
